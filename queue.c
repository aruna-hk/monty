#include "monty.h"
/**
* queue - set stack data type
* @dstruct: pointer to list
* @line_number: line number
*/
void queue(stack_t **dstruct, unsigned int line_number)
{
	nop(dstruct, line_number);

	D_TYPE = DQUEUE;
}
