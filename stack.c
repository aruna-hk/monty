#include "monty.h"
/**
* stack - set queue data struct
* @stack: pointer to tp of stack
* @line_number: opcode line number
*/
void stack(stack_t **stack, unsigned int line_number)
{
	nop(stack, line_number);

	D_TYPE = STK;
}
